### Нужно перейти в корневой файл проекта
/front

### `npm install` 
Установит зависимости приложения

### `npm start`

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

